apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - pod-disruption-budget.yaml
  - httproute.yaml
  # Tenant-owned CiliumNetworkPolicy allow-list on top of the platform's
  # default-deny floor.
  - networkpolicy.yaml
